class Student {
    constructor(name, age, course, grade) {
        (this.name = name), (this.age = age), (this.cousre = course), (this.grade = grade);
    }
    
}

class Course {
    constructor(course) {
        (this.course = course);
    }
}

class Grade {
    constructor(grade) {
        this.grade = grade;
    }
}
